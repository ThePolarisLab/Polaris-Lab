# Polaris v1.0 Punch List

Status date: 2026-07-29

## Phase 1: Security Gate

Status: In progress, not complete.

Scope:

- route-security inventory;
- backend authentication and permission enforcement;
- organization-context validation;
- production local-auth secret hardening;
- QuickBooks OAuth state binding;
- frontend session handling;
- backend and frontend security tests.

Current status:

- Draft PR #92 implements the initial Security Gate.
- Final CTO merge-readiness review found tenant-isolation blockers.
- PR #92 must not be merged until Phase 1.1 is complete and reviewed.

## Phase 1.1: Tenant Isolation Hardening

Status: Draft implementation in progress on `phase1.1/tenant-isolation-hardening`.

Scope:

- tenant ownership inventory;
- `organization_id` ownership for tenant-owned SQLAlchemy models;
- principal-derived query filters for tenant-owned reads and writes;
- QuickBooks credential, OAuth, sync, status, and financial cache ownership by organization ID;
- atomic QuickBooks OAuth state consumption;
- split read/write permissions for executive, connector, financial, identity, and organization operations;
- platform administrator behavior for all-organization administration;
- cross-organization negative tests.

Completion criteria:

- all Python tests pass;
- all TypeScript tests pass;
- frontend tests pass;
- frontend production build passes;
- Security Gate workflow passes;
- Runtime Verification workflow passes;
- route-security matrix matches implementation;
- tenant-isolation documentation matches implementation;
- repository search confirms tenant-owned queries filter by organization ID;
- draft PR #93 is reviewed and approved.

Current status:

- Tenant ownership model documented in `docs/security/tenant-isolation.md`.
- Route-security matrix updated in `docs/security/route-security-matrix.md`.
- Auth flow and environment requirements updated in `docs/security/production-auth.md`.
- Gate remains open until GitHub Actions pass on PR #93 and human review approves the changes.

## Later Phases

The following phases are intentionally out of scope for Phase 1 and Phase 1.1:

1. Database migrations and persistence lifecycle.
2. Motive production adapter.
3. Outlook production adapter.
4. API-001 response/versioning migration.
5. Deployment manifests and production runbooks.
6. CI/CD hardening beyond the checks required to validate this gate.

Do not mark Polaris v1.0 production-ready until every phase has passing automated evidence and human approval.
