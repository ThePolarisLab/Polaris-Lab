{
  "name": "polaris-lab",
  "version": "0.5.0",
  "private": true,
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "jest --runInBand",
    "verify": "npm run typecheck && npm test"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^30.0.0",
    "ts-jest": "^29.4.0",
    "typescript": "^5.8.3"
  }
}
